<div class="section">
    <div class="table-container">
            <section class="hero">
            <div class="hero-body">
                <div class="container">
                    <h1 class="title">
                        CLIENTES
                    </h1>
                </div>
            </div>
        </section>
        <table class="table">
            <thead>
                <tr>
                    <th>DPI</th>
                    <th>Nombre</th>
                    <th>Apellidos</th>
                    <th>Telefono</th>
                    <th>Email</th>
                    <th>Estado</th>
                </tr>
            </thead>
            <tbody>
                {{#each datos}}
                <tr>
                    <td>{{ dpi }}</td>
                    <td>{{ name }}</td>
                    <td>{{ lastname }}</td>
                    <td>{{ phone }}</td>
                    <td>{{ email }}</td>
                    <td>{{ is_active }}</td>
                    <td><a class="button" href="/petto/show/{{id}}">Detalle</a></td>
                    <td><a class="button is-link" href="/petto/edit/{{id}}">Editar</a></td>
                    <td>
                        <form action="/petto/destroy/{{id}}" method="POST">
                            <button class="button">Eliminar</button>
                        </form>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>