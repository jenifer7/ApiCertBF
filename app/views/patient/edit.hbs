<div class="section">
    <div class="container">
            <section class="hero">
            <div class="hero-body">
                <div class="container">
                    <h1 class="title">
                        EDITAR REGISTRO DE PACIENTE    
                    </h1>
                    <h2>{{dato/data.[0].name}}</h2>
                </div>
            </div>
        </section>
        <form action="/petto/patient/update/{{dato.id}}" method="POST">
            <div class="field">
                <label class="label">Nombre</label>
                <div class="control has-icons-left has-icons-right">
                    <input class="input is-success" type="text" placeholder="Nombre" name="name" value="{{dato/data.[0].name}}">
                    <span class="icon is-small is-right">
                        <i class="fas fa-check"></i>
                    </span>
                </div>
            </div>

            <div class="field">
                <label class="label">Especie</label>
                <div class="control has-icons-left has-icons-right">
                    <input class="input" type="text" placeholder="Especie" name="species" value="{{dato/data.[0].species}}">
                    <span class="icon is-small is-right">
                        <i class="fas fa-check"></i>
                    </span>
                </div>
            </div>

            <div class="field">
                <label class="label">Descripción</label>
                <div class="control">
                    <input class="input" name="description" type="text" value="{{dato/data.[0].description}}">
                </div>
            </div>

            <div class="field">
                <label class="label">Genero</label>
                <div class="control">
                    <input class="input" name="gender" type="text" value="{{dato/data.[0].gender}}">
                </div>
            </div>

            <div class="field">
                <label class="label">Peso</label>
                <div class="control">
                    <input class="input" name="weight" type="text" value="{{dato/data.[0].weight}}">
                </div>
            </div>

            <div class="field">
                <label class="label">Edad</label>
                <div class="control">
                    <input class="input" name="age" type="text" value="{{dato/data.[0].age}}">
                </div>
            </div>

            <div class="field">
                <label class="label">Propietario</label>
                <div class="control">
                    <input class="input" name="client_id" type="text" value="{{dato/data.[0]/client.[0].name}}" readonly>
                </div>
            </div>

            <div class="field">
                <label class="label">Estado Clinico</label>
                <div class="control">
                    <input class="input" name="patient_status_id" type="text"
                        value="{{dato/data.[0]/status.[0].name}}">
                </div>
            </div>

            <div class="field">
                <label class="label">Estado</label>
                <div class="control">
                    <input class="input" name="is_active" type="text"
                        value="{{dato/data.[0].is_active}}">
                </div>
            </div>

            <div class="field is-grouped">
                <div class="control">
                    <button class="button is-link">Guardar</button>
                </div>
                <div class="control">
                    <a href="/petto/patient/" class="button is-link is-light">Cancelar</a>
                </div>
            </div>
        </form>
    </div>
</div>