<div class="section">
    <div class="table-container">
            <section class="hero">
            <div class="hero-body">
                <div class="container">
                    <h1 class="title">
                        TIPOS DE PRODUCTOS
                    </h1>
                </div>
            </div>
        </section>
        <table class="table">
            <thead>
                <tr>
                    <th>Tipo</th>
                    <th>Descripción</th>
                    <th>Estado</th>
                </tr>
            </thead>
            <tbody>
                {{#each data}}
                <tr>
                    <td>{{ type }}</td>
                    <td>{{ description }}</td>
                    <td>{{ is_active }}</td>
                    <td><a class="button" href="/petto/types_pro/show/{{id}}">Detalle</a></td>
                    <td><a class="button is-link" href="/petto/types_pro/edit/{{id}}">Editar</a></td>
                    <td>
                        <form action="/petto/types_pro/destroy/{{id}}" method="POST">
                            <button class="button">Eliminar</button>
                        </form>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>