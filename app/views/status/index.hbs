<div class="section">
    <div class="container">
            <section class="hero">
            <div class="hero-body">
                <div class="container">
                    <h1 class="title">
                        ESTADOS
                    </h1>
                </div>
            </div>
        </section>
        <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Nombre</th>
                    <th>Descripción</th>
                </tr>
            </thead>
            <tbody>
                {{#each statuses}}
                <tr>
                    <td>{{ id }}</td>
                    <td>{{ name }}</td>
                    <td>{{ description }}</td>
                    <td>{{ is_active }}</td>
                    <td><a class="button" href="/petto/status/show/{{id}}">Detalle</a></td>
                    <td><a class="button is-link" href="/petto/status/edit/{{id}}">Editar</a></td>
                    <td>
                        <form action="/petto/status/destroy/{{id}}" method="POST">
                            <button class="button">Eliminar</button>
                        </form>
                    </td>
                </tr>
                {{/each}}
            </tbody>
        </table>
    </div>
</div>